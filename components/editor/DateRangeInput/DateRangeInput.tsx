import React from 'react';
import * as Styled from './DateRangeInput.styled';

interface DateRangeInputProps {}

export default function DateRangeInput({}: DateRangeInputProps) {
  return <Styled.Container>DateRangeInput</Styled.Container>;
}
