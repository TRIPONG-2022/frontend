import React from 'react';
import * as Styled from './PublishModal.styled';

interface PublishModalProps {}

export default function PublishModal({}: PublishModalProps) {
  return <Styled.Container></Styled.Container>;
}
